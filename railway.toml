[deploy]
startCommand = "uv run gunicorn app.main:app -k uvicorn.workers.UvicornWorker --timeout 120"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
maxRestartCount = 10
